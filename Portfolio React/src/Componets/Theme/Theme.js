// theme.js
import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  colors: {
    customYellow: 'rgba(245, 195, 31, 1)', 
    custombg: 'rgba(49,51,59,255)',
  },
});

export default theme;
